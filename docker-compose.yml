services:
  finance-app:
    build: .
    container_name: finance-app
    ports:
      - "17432:8000"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./app:/app/app
      - ./scripts:/app/scripts
    restart: unless-stopped